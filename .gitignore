node_modules
dist
src-tauri/target

# Participant / test study output — never commit (IRB)
ratings.csv
transitions.csv
123_123_123_123/

# The full stimulus library (2000+ clips, ~1.7 GB). Never committed — the app
# reads it from a folder chosen on the dashboard. The eight proof-of-concept
# clips in public/videos ARE committed, so a fresh clone and the CI installers
# work with no setup; `npm run stimuli` repopulates them from this folder.
mp4_noname/
